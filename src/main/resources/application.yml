spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen-plus
          temperature: 0.7
          top-p: 0.9

tencent:
  search:
    api-key: ${TENCENT_WSA_API_KEY}
    endpoint: https://api.wsa.cloud.tencent.com/SearchPro
    timeout: 5000

server:
  port: 8080
